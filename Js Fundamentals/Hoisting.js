let a = 1;
console.log(  sum(a , 2))


function sum(a , b){
    return a + b
}

//Hoisting takes all the functions and take it to the top so even if you call a function before its defined the code would still work
// This is however different for arow functions because arrow functions are mostly difined as variables and variables are never hoisted


