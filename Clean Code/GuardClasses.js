/**
 * Guard classes are utility classes designed to enforce constraints or validate inputs
 * before proceeding with further logic. They act as a safeguard to ensure that the 
 * provided data meets specific requirements, preventing potential errors or unexpected 
 * behavior in the application.
 *
 * These classes typically contain static methods that throw errors or return specific 
 * results when the input does not meet the expected criteria. By centralizing validation 
 * logic in guard classes, the code becomes more modular, reusable, and easier to maintain.
 *
 * Example usage of guard classes:
 * - Validating user input before processing it.
 * - Ensuring that required parameters are not null or undefined.
 * - Checking that a value falls within an acceptable range.
 */



function startTimer(time){
    if ( !timer.anabled) return


}

// This is the kinna the code that I wrote in the PRG test
// Most the time that yoou see if-else functions then the possibility of you having to make use a guard class is high
// Guard classews also prevent a bunch of nesting thst we often have in our code